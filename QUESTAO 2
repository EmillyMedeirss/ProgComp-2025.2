qntMurilos = 0
numero1 = 0 # Variável de controle: Armazena o número 'n' que gerou o recorde de murilos.

for n in range (1, 1001): # INÍCIO do laço principal. 'n' representa o número principal (de 1 a 10000)
    somaN = (n * (n+1)) // 2 # Cálculo da Soma de 1 até 'n' (S_n). Ocorre 10.000 vezes.

    contagemMurilos = 0  # Contador local que é zerado ANTES de testar os 'm's.

    for m in range (1, 1001): # Laço secundário para testar os candidatos a "murilo" (m).
        if somaN % m == 0: # Verifica se o último valor de 'somaN' (de n=10000) é divisível por 'm'.
                contagemMurilos += 1 # Se for divisível, incrementa o contador.

    if contagemMurilos > qntMurilos:
        qntMurilos = contagemMurilos
        numero1 = n

print(f'O número com mais "murilos" é {numero1}, com {qntMurilos} "murilos".')