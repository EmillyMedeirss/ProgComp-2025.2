''' i = ?
*
**
***
****
***
**
*
usando while e usando for '''

'''com while'''
intPos = 4 #tamanho maximo de caracters
i = 1 # Criamos e iniciamos a nossa variável de contagem 'i' com o valor 1.

while i <=intPos: # O loop continua REPETINDO enquanto 'i' for menor ou igual a 4.
    print('*' * i) # Nesta linha, multiplicamos a string '*' pelo valor de 'i'.
    i += 1 # ESSENCIAL no 'while'! Precisamos aumentar o valor de 'i' manualmente a cada repetição.
i = intPos - 1 # Para começar a diminuir, precisamos 'resetar' o nosso contador 'i'.
# Queremos começar com 3 asteriscos (intPos - 1 = 3).
while i>= 1: # O loop continua REPETINDO enquanto 'i' for maior ou igual a 1.
    print('*' * i) # Imprime o número correto de asteriscos
    i -=1 # Precisamos diminuir o valor de 'i' manualmente a cada repetição para que o loop pare quando 'i' chegar a 0.

'''com for'''
intPos = 4 #tamanho maximo de caracters
for i in range(1, intPos + 1): # O loop 'for' irá contar para nós de 1 até 4. O 'range(1, 5)' gera os números 1, 2, 3 e 4.
    print('*' * i) # Nesta linha, multiplicamos a string '*' pelo valor de 'i'.
for i in range (intPos -1, 0, -1): # Passo: -1 (diminui 1 a cada repetição)
    print('*' * i) # Nesta linha, multiplicamos a string '*' pelo valor de 'i'.
