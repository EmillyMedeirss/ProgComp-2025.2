import sys # Importa o módulo 'sys', que permite interagir com o ambiente de execução,
            # como, por exemplo, encerrar o programa.

# Solicita ao usuário que insira um número inteiro, positivo e de no máximo 4 dígitos (1 a 9999).
# A função 'int()' converte a entrada do usuário (que é texto) para um número inteiro.
intNum = int(input('Informe um número inteiro, positivo, de até 4 dígitos:'))

# --- Validação da Entrada ---
# Verifica se o número inserido é inválido, ou seja, se for maior que 9999 OU menor que 1.
if(intNum > 9999 or intNum < 1):
    # Se for inválido, exibe uma mensagem de erro e encerra o programa.
    sys.exit('Número inválido! Informe um número entre 1 e 9999.')

# --- Extração do Milhar ---
# O operador '//' (divisão inteira) retorna apenas a parte inteira do resultado da divisão.
# intNum // 1000: Pega o dígito da casa do milhar. Ex: 1234 // 1000 = 1
intMilhar = intNum // 1000
# O operador '%' (módulo) retorna o resto da divisão.
# intNum % 1000: 'Remove' o milhar e guarda o resto do número. Ex: 1234 % 1000 = 234
intNum = intNum % 1000 # O número agora é 234 (o resto da divisão por 1000)

# --- Extração da Centena ---
# intNum // 100: Pega o dígito da casa da centena do número restante. Ex: 234 // 100 = 2
intCentena = intNum // 100
# intNum % 100: 'Remove' a centena e guarda o resto. Ex: 234 % 100 = 34
intNum = intNum % 100 # O número agora é 34 (o resto da divisão por 100)

# --- Extração da Dezena ---
# intNum // 10: Pega o dígito da casa da dezena do número restante. Ex: 34 // 10 = 3
intDezena = intNum // 10
# intNum % 10: 'Remove' a dezena e guarda o resto. Ex: 34 % 10 = 4
intNum = intNum % 10 # O número agora é 4 (o resto da divisão por 10)

# --- Extração da Unidade ---
# intNum // 1: Pega o dígito da unidade do número restante. Ex: 4 // 1 = 4
intUnidade = intNum // 1
# intNum % 1: O resto da divisão por 1 é sempre 0, pois já pegamos a unidade.
# Ex: 4 % 1 = 0
intNum = intNum % 1 # O número agora é 0 (não tem mais dígitos)

# --- Soma dos Dígitos ---
# Soma todos os dígitos que foram extraídos em cada etapa.
# O 'intNum' final (que é 0) é incluído na soma, mas não altera o resultado.
soma = intMilhar + intCentena + intDezena + intUnidade + intNum

# Exibe o resultado da soma dos dígitos na tela.
print(soma)